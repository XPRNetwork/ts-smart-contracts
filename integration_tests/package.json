{
  "scripts": {
    "build:createnft": "npx proton-asc ./createnft/createnft.contract.ts",
    "test:createnft": "mocha -s 250 ./createnft/*.spec.ts -r ts-node/register",
    "test": "npm run build:createnft && npm run test:createnft"
  },
  "dependencies": {
    "proton-tsc": "file:.."
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^9.2.2"
  }
}
