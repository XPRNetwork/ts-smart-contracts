import { ActionWrapper, Name } from ".."

/**
 * The name of the constant and the string must be exactly the same
 * for decorators to utilize it correctly
 */
// External
export const atomicassets = Name.fromString("atomicassets");